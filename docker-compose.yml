services:
  api-server:
    build:
      context: .
      dockerfile: apps/api-server/Dockerfile
    ports:
      - "4000:4000"
    env_file:
      - apps/api-server/.env
    environment:
      PORT: 4000
      NODE_ENV: production
      # Must include every origin that loads the admin SPA (comma-separated)
      CORS_ORIGIN: "http://localhost:5173,http://localhost:80,https://nks.sena.my"
      SESSION_SECRET: "${SESSION_SECRET:-change-me-in-production}"
      SESSION_TTL_HOURS: 24
      UPLOAD_DIR: uploads
      MAX_UPLOAD_MB: 5
      ADMIN_EMAIL: "${ADMIN_EMAIL:-admin@example.com}"
      ADMIN_PASSWORD: "${ADMIN_PASSWORD:-admin12345}"
      JAN_PGP_PASSWORD: "${JAN_PGP_PASSWORD:-nks-jan-dev-2025}"
    volumes:
      - api_uploads:/app/apps/api-server/uploads

  admin-web:
    build:
      context: .
      dockerfile: apps/admin-web/Dockerfile
      args:
        # Option A same-origin: empty = relative /api/... (works with nginx proxy below)
        # Production Coolify: set to https://nks.sena.my so SPA hits same host
        VITE_API_BASE_URL: ""
    ports:
      - "5173:80"
    depends_on:
      - api-server

volumes:
  api_uploads:
